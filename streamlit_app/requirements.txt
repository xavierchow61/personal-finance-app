# Streamlit Cloud 專用 requirements
# 只包含網頁版需要的套件，排除 Windows 專用 / 桌面專用
# 部署時 Streamlit Cloud 會優先讀 streamlit_app/requirements.txt（而非 repo 根目錄那個）

# === 核心 Web 框架 ===
streamlit==1.57.0

# === 資料處理 ===
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
pytz==2026.2

# === 視覺化 ===
plotly==6.7.0

# === AI / OCR ===
google-genai==2.6.0

# === 環境變數 ===
python-dotenv==1.2.2

# === Excel 匯出 ===
openpyxl==3.1.5
xlsxwriter==3.2.9

# === 圖片處理（單據預覽 + AI extractor）===
Pillow==12.2.0

# === PDF 處理（純 Python，不需 poppler）===
pypdfium2==5.8.0
