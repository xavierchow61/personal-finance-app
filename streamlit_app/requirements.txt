# Streamlit Cloud 專用 requirements
# 用 >= 而非 ==，等 pip 自動揀 Python 3.13 Linux wheel
# 避免從源碼編譯（pandas 編譯需要 5-10 分鐘，可能 fail）

# === 核心 Web 框架 ===
streamlit>=1.57.0

# === 資料處理（>=2.2.4 開始有完整 Python 3.13 wheels）===
pandas>=2.2.4
numpy>=2.2.0
pyarrow>=18.0.0
pytz>=2024.2

# === 視覺化 ===
plotly>=6.0.0

# === AI / OCR ===
google-genai>=2.0.0

# === 環境變數 ===
python-dotenv>=1.0.0

# === Excel 匯出 ===
openpyxl>=3.1.0
xlsxwriter>=3.2.0

# === 圖片處理（單據預覽 + AI extractor）===
Pillow>=11.0.0

# === PDF 處理（純 Python，不需 poppler）===
pypdfium2>=4.0.0

# === Supabase / PostgreSQL（雲端 DB）===
psycopg[binary]>=3.2.0
